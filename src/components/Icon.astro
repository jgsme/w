---
import type { Node } from "@progfay/scrapbox-parser";
interface Props {
  node: Node;
}
const { node } = Astro.props;
---

{
  node.type === "icon" &&
    node.pathType === "root" &&
    node.path === "/icons/hr" && <hr />
}
